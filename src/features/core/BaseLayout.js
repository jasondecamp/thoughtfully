import React, { Component } from 'react';

export default class BaseLayout extends Component {
  static propTypes = {

  };

  render() {
    return (
      <div className="core-base-layout">
        Component content: core/BaseLayout
      </div>
    );
  }
}
